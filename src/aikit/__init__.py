"""aikit — shared platform layer for AI-backend systems.

Public interfaces (SOLID contracts) live here. Concrete implementations are
optional and gated behind extras so consumers pull only what they use.
"""
__version__ = "0.2.0"
